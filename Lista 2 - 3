#include <stdio.h>

int main ()
{
    int n, x, y;
    
    scanf("%i", &n);
    
    
    for (x = 0; x < n; x++)
    {
        for (y = 0; y < x; y++)
        if (x + y == n)
        {
            printf("%i + %i = %i\n", x, y, n);
        }
        
    }
    
   
    return 0;
}
